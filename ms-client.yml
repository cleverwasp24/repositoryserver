management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  data:
    mongodb:
      url: '27017'
      auto-index-creation: 'true'
      database: msclient
      host: localhost
  application:
    name: ms-client
logging:
  level:
    com:
      nttdata:
        bootcamp:
          msclient: info
server:
  port: '8081'
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8089/eureka/
