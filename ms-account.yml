management:
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  data:
    mongodb:
      url: '27017'
      auto-index-creation: 'true'
      database: msaccount
      host: localhost
  application:
    name: ms-account
logging:
  level:
    com:
      nttdata:
        bootcamp:
          msaccount: info
server:
  port: '8082'
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8089/eureka/
